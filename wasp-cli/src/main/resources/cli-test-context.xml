<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int-ip="http://www.springframework.org/schema/integration/ip"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:int="http://www.springframework.org/schema/integration"
	xsi:schemaLocation="http://www.springframework.org/schema/integration/ip http://www.springframework.org/schema/integration/ip/spring-integration-ip.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- client -->
	
	<context:annotation-config />
	<context:component-scan base-package="edu.yu.einstein.wasp.cli" />
	<int:gateway id="clientGateway" service-interface="edu.yu.einstein.wasp.cli.TcpGateway"
		default-request-channel="inputChannel" />

	<int-ip:tcp-connection-factory id="clientFactory"
		type="client" 
		host="localhost" 
		port="33333" 
		single-use="true"
		so-timeout="10000" />

	<int:channel id="inputChannel" />

	<int-ip:tcp-outbound-gateway id="outboundGateway"
		request-channel="inputChannel" 
		reply-channel="replyChannel"
		connection-factory="clientFactory" 
		request-timeout="10000"
		reply-timeout="10000" />

	<int:transformer id="clientBytes2String" 
		input-channel="replyChannel"
		expression="new String(payload)" />

	<!-- test server -->

	<int-ip:tcp-connection-factory id="testServer"
		type="server" 
		port="33333" />
		
	<int-ip:tcp-inbound-gateway id="testGateway"
		connection-factory="testServer" 
		request-channel="requestChannel"
		error-channel="errorChannel" />
		
	<int:channel id="toServiceActivator" />
	
	<int:service-activator input-channel="toServiceActivator" 
		ref="echoService"
		method="echo" />
		
	<bean id="echoService"
		class="edu.yu.einstein.wasp.cli.EchoService" />
		
	<int:transformer id="serverBytes2String" input-channel="requestChannel"
		output-channel="toServiceActivator" expression="new String(payload)" />
		
	<int:transformer id="errorHandler" input-channel="errorChannel"
		expression="payload.failedMessage.payload + ':' + payload.cause.message" />

</beans>
