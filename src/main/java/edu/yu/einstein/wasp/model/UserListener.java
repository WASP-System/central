package edu.yu.einstein.wasp.model;

import javax.persistence.*;

public class UserListener {

/*
  User u = null;

  @PostLoad
  public void loadUser (Object entity) {
    System.out.println("LOAD USER" + entity);

    // stores clone of original
    User u = (User) entity;
    this.u = u.clone();
  }

  @PostPersist
  public void insertUserHistory (Object entity) {
    System.out.println("INSERT USER HISTORY" + entity);
  }

  @PostUpdate
  public void updateUserHistory (Object entity) {
    System.out.println("UPDATE USER HISTORY" + entity);

    User u = (User) entity; 

    if (u.__original.login.equals(u.getLogin())) {  
      System.out.println("same login");
    } else { 
      System.out.println("login old:'" + u.__original.login + "' new:" + u.getLogin());
    }
    if (u.__original.email.equals(u.getEmail())) {  
      System.out.println("same email");
    } else { 
      System.out.println("email old:'" + u.__original.email + "' new:" + u.getEmail());
    }
    if (u.__original.firstName.equals(u.getFirstName())) {  
      System.out.println("same firstName");
    } else { 
      System.out.println("firstName old:'" + u.__original.firstName + "' new:" + u.getFirstName());
    }

  }

*/
}
