<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.1.xsd">

	<bean id="batchDataSource" class="org.apache.commons.dbcp.BasicDataSource">
		<property name="driverClassName"><value>${batchdb.jdbc.driver}</value></property>
		<property name="url"><value>${batchdb.jdbc.url}</value></property>
		<property name="username"><value>${batchdb.jdbc.username}</value></property>
		<property name="password"><value>${batchdb.jdbc.password}</value></property>
		<property name="maxIdle"><value>${batchdb.jdbc.maxIdle}</value></property>
		<property name="initialSize"><value>${batchdb.jdbc.initialSize}</value></property>
   		<property name="maxActive"><value>${batchdb.jdbc.maxActive}</value></property>
   		<property name="maxWait"><value>${batchdb.jdbc.maxWait}</value></property>
   		<property name="testOnBorrow"><value>${batchdb.jdbc.testOnBorrow}</value></property>
		<property name="validationQuery"><value>${batchdb.jdbc.validationQuery}</value></property>
	</bean>
	
	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
		<property name="driverClassName"><value>${db.jdbc.driver}</value></property>
		<property name="url"><value>${db.jdbc.url}</value></property>
		<property name="username"><value>${db.jdbc.username}</value></property>
		<property name="password"><value>${db.jdbc.password}</value></property>
		<property name="defaultAutoCommit"><value>${db.jdbc.auto_commit}</value></property>
		<property name="maxIdle"><value>${db.jdbc.maxIdle}</value></property>
		<property name="initialSize"><value>${db.jdbc.initialSize}</value></property>
   		<property name="maxActive"><value>${db.jdbc.maxActive}</value></property>
   		<property name="maxWait"><value>${db.jdbc.maxWait}</value></property>
   		<property name="testOnBorrow"><value>${db.jdbc.testOnBorrow}</value></property>
		<property name="validationQuery"><value>${db.jdbc.validationQuery}</value></property>
	</bean>

	<bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
		<property name="persistenceUnitName" value="waspPU" />
		<property name="dataSource" ref="dataSource" />
		<property name="jpaVendorAdapter">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
				<property name="database"><value>${db.jdbc.database}</value></property>
				<property name="showSql"><value>${db.jdbc.show_sql}</value></property>
				<property name="generateDdl"><value>${db.jdbc.hbm2ddl_auto}</value></property>
			</bean>
		</property>   
		<property name="packagesToScan" value="edu.yu.einstein.wasp.model" />
	</bean>
	
	<!-- use @Transactional("entityManager") for transactions on the core database (e.g. service methods in the core) -->
	<bean name="transactionManagerEM" class="org.springframework.orm.jpa.JpaTransactionManager">
	  <property name="entityManagerFactory" ref="entityManagerFactory" />
	  <qualifier value="entityManager"/>
	</bean>
	
	<!-- default (batch database transactions) -->
	<bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="batchDataSource" />
	</bean>
	
	<!-- Enable @Transactional support -->
	<tx:annotation-driven />
	<bean class="org.springframework.orm.jpa.support.PersistenceAnnotationBeanPostProcessor" /> 

</beans>
