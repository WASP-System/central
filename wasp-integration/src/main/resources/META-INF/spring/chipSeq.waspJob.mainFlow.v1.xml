<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:batch="http://www.springframework.org/schema/batch"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch.xsd">
		
	<!-- common tasklets defined in config-common-context.xml -->
	
	<batch:flow id="chipSeq.waspJob.flow.approvals.v1" >
		<batch:split id="chipSeq.waspJob.postSubmitTasks" task-executor="taskExecutor">
			<batch:flow>
				<batch:step id="chipSeq.waspJob.quote">
					<batch:tasklet ref="listenForJobQuotedTasklet"/>
				</batch:step>
			</batch:flow>
			<batch:flow>
				<batch:step id="chipSeq.waspJob.piApprove">
					<batch:tasklet ref="listenForJobPiApproveTasklet" />
				</batch:step>
			</batch:flow>
			<batch:flow>
				<batch:step id="chipSeq.waspJob.adminApprove">
					<batch:tasklet ref="listenForJobAdminApproveTasklet" />
				</batch:step>
			</batch:flow>
			<batch:next on="COMPLETED" to="chipSeq.waspJob.approved" />
			<batch:next on="FAILED" to="chipSeq.waspJob.rejected" />
			<batch:fail on="*"/> <!-- catch all -->
		</batch:split>
		<batch:step id="chipSeq.waspJob.approved">
			<batch:tasklet>
				<bean class="edu.yu.einstein.wasp.tasklets.NotifyStatusTasklet" scope="step">
					<constructor-arg name="outputMessageChannel" ref="wasp.channel.batch" />
					<constructor-arg name="messageTemplate">
						<bean class="edu.yu.einstein.wasp.messages.WaspJobStatusMessageTemplate">
							<constructor-arg  name="jobId" value="#{jobParameters['jobId']}"/>
							<property name="task" value="#{jobTasks.NOTIFY_STATUS}" />
							<property name="status" value="CREATED" />
						</bean>
					</constructor-arg>
					
				</bean>
			</batch:tasklet>
		</batch:step>
		<batch:step id="chipSeq.waspJob.rejected">
			<batch:tasklet>
				<bean class="edu.yu.einstein.wasp.tasklets.NotifyStatusTasklet" scope="step">
					<constructor-arg name="outputMessageChannel" ref="wasp.channel.batch" />
					<constructor-arg name="messageTemplate">
						<bean class="edu.yu.einstein.wasp.messages.WaspJobStatusMessageTemplate">
							<constructor-arg  name="jobId" value="#{jobParameters['jobId']}"/>
							<property name="task" value="#{jobTasks.NOTIFY_STATUS}" />
							<property name="status" value="ABANDONED" />
						</bean>
					</constructor-arg>
				</bean>
			</batch:tasklet>
		</batch:step>
	</batch:flow>
	
	<!-- for job, call with parameter 'jobId' -->
	<batch:job id="chipSeq.waspJob.jobflow.v1">
		<batch:split id="chipSeq.waspJob.jobFlow.split" task-executor="taskExecutor">
			<batch:flow parent="chipSeq.waspJob.flow.approvals.v1" />
			<batch:flow>
				<!-- will receive message of COMPLETED if the 'chipSeq.waspJob.flow.approvals.v1' flow is finished -->
				<!-- or if the job is aborted with an ABANDONED message at time e.g. via a message from the web layer -->
				<batch:step id="chipSeq.waspJob.jobflow.ListenForComplete">
					<batch:tasklet ref="listenForJobFinishedStatusTasklet" />
				</batch:step>
			</batch:flow>
			<batch:end on="COMPLETED" exit-code="COMPLETED" />
			<batch:end on="FAILED" exit-code="ABANDONED" />
			<batch:fail on="*"/> <!-- catch all -->
		</batch:split>
	</batch:job>

</beans>
