<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:batch="http://www.springframework.org/schema/batch"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch.xsd">
		
	<!-- common tasklets defined in config-common-context.xml -->
	
	<bean id='jobTasks' class="edu.yu.einstein.wasp.messages.WaspJobTask" />

	<batch:flow id="chipSeq.waspJob.flow.approvals.v1" >
		<batch:split id="chipSeq.waspJob.postSubmitTasks" task-executor="taskExecutor">
			<batch:flow>
				<batch:step id="chipSeq.waspJob.quote">
					<batch:tasklet ref="waspJobQuoteTasklet" />
				</batch:step>
			</batch:flow>
			<batch:flow>
				<batch:step id="chipSeq.waspJob.piApprove">
					<batch:tasklet ref="waspJobPiApproveTasklet" />
				</batch:step>
			</batch:flow>
			<batch:flow>
				<batch:step id="chipSeq.waspJob.adminApprove">
					<batch:tasklet ref="waspJobAdminApproveTasklet" />
				</batch:step>
			</batch:flow>
			<!-- if any of the split flows return an exit status of FAILED (i.e. rejected) then proceed down rejected pathway -->
			<batch:next on="FAILED" to="chipSeq.waspJob.rejected" />
			<batch:next on="COMPLETED" to="chipSeq.waspJob.approved" />
			<batch:fail on="*"/> <!-- unexpected message -->
		</batch:split>
		<batch:step id="chipSeq.waspJob.approved">
			<batch:tasklet>
				<bean class="edu.yu.einstein.wasp.tasklets.job.ChipSeqJobActionedTasklet" scope="step">
					<constructor-arg name="jobId" value="#{jobParameters['jobId']}" />
					<constructor-arg name="outputMessageChannel" ref="wasp.channel.batch" />
					<constructor-arg name="isAccepted" value="true" />
				</bean>
			</batch:tasklet>
		</batch:step>
		<batch:step id="chipSeq.waspJob.rejected">
			<batch:tasklet>
				<bean class="edu.yu.einstein.wasp.tasklets.job.ChipSeqJobActionedTasklet" scope="step">
					<constructor-arg name="jobId" value="#{jobParameters['jobId']}" />
					<constructor-arg name="outputMessageChannel" ref="wasp.channel.batch" />
					<constructor-arg name="isAccepted" value="false" />
				</bean>
			</batch:tasklet>
			<batch:end on="*" exit-code="ABANDONED"/>
		</batch:step>
	</batch:flow>
	
	<!-- for job, call with parameter 'jobId' -->
	<batch:job id="chipSeq.waspJob.jobflow.v1">
		<batch:flow id="chipSeq.waspJob.jobflow.approvals" parent="chipSeq.waspJob.flow.approvals.v1" />
	</batch:job>

</beans>
